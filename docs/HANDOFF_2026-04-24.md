# Data Input Workspace — Handoff 2026-04-24

**State:** typecheck EXIT=0 · dev server running · UI usable dengan optimistic config staging.

## Yang Sudah Jalan Sesi Ini

### Visual / UX
- 3-section layout (breadcrumb / title / toolbar) dengan vertical rhythm py-2.5 konsisten
- Breadcrumb: `Data Input › Master_Data › Bay` — clean path, `›` separator, last item font-medium
- Title: H1 `ds-heading` 24px + BookKey h-5
- Empty state "No rows yet" dengan CTA + "No matching rows" dengan Clear filters
- Loading state: bigger loader + helper text
- Zebra striping alternate rows (native CSS `:nth-child`)
- Row hover neutral state + selected state no-double-highlight
- Column drag cursor-grab + dropzone accent bar animate pulse
- Marker differentiation: `*` red (required) · `🔗` amber (ancestor virtual) · `🔗` sky (reference FK)
- Chevron kolom rotate 180° smooth saat menu open
- StatusChip toolbar (Filters/Sort only, auto-hide count=0)
- Cascading menu (Sort/Filter submenu hover)
- Filter inline submenu (UPT/ULTG ancestor juga bisa difilter)
- Save button amber `ds-btn-primary` (bukan emerald clash)
- Bulk delete via keyboard Delete key + footer hint
- English enterprise bahasa

### Optimistic Config Staging (NEW)
- Drag column → langsung pindah visual (local state `pendingConfig`)
- Resize column → langsung visual berubah
- Toolbar pill "N unsaved" muncul saat pending > 0
- Save config button → batch PATCH ke Firestore
- Discard button → revert

### Refactor Phase B — SHIPPED 2026-04-24 malam
Extracted dari monolith 1897 baris → **MasterGrid.tsx 1066 lines** (−831, −44%):
- `grid-utils.ts` — estimateWidth, formatRelativeTime, formatDate
- `cell-editors.tsx` — Bool/Number/Choice/Reference editors
- `cell-renderers.tsx` — renderCellView + renderEditor dispatchers
- `HeaderCellMinimal.tsx` — header cell generic
- `ColumnHeaderPopover.tsx` (+ PopoverItem, ColumnMenuEntry)
- `ExportMenuPopup.tsx` (+ ExportMenuItem)
- `StatusChip.tsx` + `StatusPopup.tsx`
- `hooks/useCellSelection.ts` — selectedRows + handleCellClick
- `hooks/useSortState.ts` — sortColumns + toggleSort + sortRows (__ancestor aware)
- `hooks/useColumnFilters.ts` — filters + search + filteredRows
- `hooks/useDraftMode.ts` — rows + dirtyMap + undo/redo + save/discard + cell events + rowClass
- Orphan `ColumnHeaderMenu.tsx` removed (dead code)
- SortIconButton removed (dead code)

Typecheck bersih. Target <400 belum tercapai (masih 1066) — sisa besar = rdgColumns builder + JSX render. Kandidat iter berikutnya:
- Ekstrak builder kolom (`buildColumnMenuEntries(col, ctx)` registry factory)
- Pisah render sections (toolbar, grid wrapper, empty states, popups)

## Yang Belum

### P0 Backend (per audit docs/AUDIT_BE_DATA_INPUT.md)
1. Buat `platform_internal.audit_log` BQ table (dataset sudah dibuat)
2. Cross-dataset FK scan (bq-discovery.ts:201) — expand ke Master_*/Dashboard_*
3. Hapus hardcode MASTER_FK di columns/route.ts:17-22
4. Guard DELETE dataset origin=platform

### Pending User Feedback
- Task #40: Drag column UX god-mode polish (dnd-kit migration kalau native RDG visual kurang)
- Task #32: Test non-Master_Data table (n_MTU_CT dll) — uncover hardcoded assumption

## File Inventory

```
_workspace/
├── MasterGrid.tsx              1066 lines (target <400, progress 44%)
├── cell-editors.tsx            152
├── cell-renderers.tsx           83
├── grid-utils.ts                80
├── HeaderCellMinimal.tsx       110
├── ColumnHeaderPopover.tsx     195 (NEW — + PopoverItem + ColumnMenuEntry)
├── ExportMenuPopup.tsx         105 (NEW — + ExportMenuItem)
├── StatusChip.tsx              155 (NEW — + StatusPopup)
├── hooks/
│   ├── useCellSelection.ts     ~40  (NEW)
│   ├── useSortState.ts         ~65  (NEW)
│   ├── useColumnFilters.ts     ~75  (NEW)
│   └── useDraftMode.ts         ~285 (NEW — biggest, draft state + events)
├── ColumnSidePanel.tsx         650
├── BulkImportModal.tsx         640
├── WorkspaceDrawer.tsx         130
├── ToolRail.tsx                 85
├── SheetFilterPopup.tsx        290 (+ SheetFilterBody export)
├── AddColumnModal.tsx
├── sidebar-layout.ts            65
└── types.ts                    130

docs/
├── DATA_WORKSPACE_DESIGN-iteasi dengan claude dekstop.md  (canonical spec)
├── DATA_INPUT_AUDIT.md
├── AUDIT_BE_DATA_INPUT.md      (audit backend, 3 P0 critical)
├── MASTERGRID_DECOMPOSITION.md (12-step refactor blueprint)
└── HANDOFF_2026-04-24.md       (THIS FILE)
```

## Next Session Plan (Priority Order)

1. **Finish Phase B refactor** per blueprint (2-3 jam silent work)
2. **Test hands-on** real data entry flow
3. **P0 Backend** — audit_log + FK cross-dataset + hardcode removal
4. **Test non-Master tables** — uncover hardcode per-table assumption

## Decision Log

- Per-table config (bukan per-user) — user confirm 2026-04-24
- RDG native drag + reorder + resize dengan optimistic local state + batch Save
- Bulk action via keyboard (Delete) bukan Floating Action Bar
- Chevron button = satu-satunya trigger column menu (no click header auto-sort)
- English enterprise language (Airtable/Supabase convention)
